# Trivy image-scan exceptions (Jenkinsfile stage 8) - trivy runs from the repo root, so this
# file applies to both the backend and frontend scans in one place.
#
# Every entry below was verified against Maven Central/upstream on the date noted: the fix
# version Trivy's vulnerability DB reports does not exist in any published repository yet, so
# there is no dependency bump that resolves it today. Re-run `trivy image` periodically and
# remove entries here once a real fix becomes available - this file should shrink over time,
# not grow.

# NOTE: alpine-level CVEs (libexpat, p11-kit, etc.) are NOT listed here - both Dockerfiles now
# run `apk upgrade` at build time, which pulls the patched packages directly from Alpine's repo
# instead of waiting for the upstream base images to be rebuilt. If a future scan still flags
# an alpine package, check whether the fix is actually published (pkgs.alpinelinux.org) before
# assuming apk upgrade should have caught it.

# jackson-core / jackson-databind - fix versions 2.18.8/2.21.4/2.22.1/3.1.4 not yet published
# (latest available: 2.19.1, already in use via Spring Boot 3.5.3's BOM)
GHSA-r7wm-3cxj-wff9
CVE-2026-54512
CVE-2026-54513

# org.postgresql:postgresql - fix 42.7.11 not yet published (latest available: 42.7.7, in use)
CVE-2026-42198
CVE-2026-54291

# org.springframework.boot:spring-boot - fix 3.5.14/4.0.6 not yet published (latest: 3.5.3, in use)
CVE-2026-40973

# org.springframework.data:spring-data-commons - fix 3.5.12/4.0.6 not yet published (latest: 3.5.1)
CVE-2026-41695

# org.springframework.security - fix versions 6.4.10/6.5.4/6.5.9/7.0.4 not yet published
# (latest available: 6.5.1, in use via Spring Boot 3.5.3's BOM)
CVE-2025-41248
CVE-2026-22732

# org.springframework:spring-core / spring-expression / spring-webmvc - fix 6.2.11/6.2.19/7.0.8
# not yet published (latest available: 6.2.8, in use via Spring Boot 3.5.3's BOM)
CVE-2025-41249
CVE-2026-41850
CVE-2026-41845
CVE-2026-41842
